# ─── Deploy App Environment Variables ─────────────────────────────────────────
# Copy to the deploy app's runtime directory:
#   /srv/apps/deploy/.env  (on the server)
#
# These are NOT used during build — they're injected at container runtime.

# ─── AWS S3 — Server Backups & Image Archival ─────────────────────────────────
# Used to push Docker image tarballs and database backups to S3.
# IAM user: matrx-server-backup (programmatic access only)
# Policy: MatrxServerBackupS3Access (scoped to the backup bucket)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
S3_BACKUP_BUCKET=matrx-server-backups-prod
