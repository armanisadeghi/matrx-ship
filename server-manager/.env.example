# ─── Server Manager Environment Variables ──────────────────────────────────────
# Copy to the server-manager runtime directory:
#   /srv/apps/server-manager/.env  (on the server)
#
# These are NOT used during build — they're injected at container runtime.

# ─── Authentication ────────────────────────────────────────────────────────────
# Comma-separated list of valid API tokens for manager access
# Generate secure tokens with: openssl rand -hex 32
# Example: MANAGER_TOKENS=token1,token2,token3
MANAGER_TOKENS=

# Legacy single token support (deprecated, use MANAGER_TOKENS instead)
# MANAGER_BEARER_TOKEN=

# ─── Supabase Integration (Optional) ───────────────────────────────────────────
# For dual-write persistence and cross-server sync
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ─── AWS S3 — Server Backups & Image Archival ─────────────────────────────────
# Used to push Docker image tarballs and database backups to S3.
# IAM user: matrx-server-backup (programmatic access only)
# Policy: MatrxServerBackupS3Access (scoped to the backup bucket)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
S3_BACKUP_BUCKET=matrx-server-backups-prod
