# .matrx-tools.conf — Project configuration (legacy format)
# Docs: https://github.com/armanisadeghi/matrx-ship/tree/main/cli

# Project type: "node" or "python"
PROJECT_TYPE="node"

# ─── Tools to install ───────────────────────────────
TOOLS_ENABLED="env-sync"

# ─── Env Sync Configuration ─────────────────────────
DOPPLER_PROJECT="matrx-ship"
DOPPLER_CONFIG="prd"
ENV_FILE=".env"
